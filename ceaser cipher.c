#include<stdio.h>
#unclude<string.h>
#include<conio.h>
#include<ctype.h>
void main()
{
	char plain[10],cipher[10];
	int key,i,length;
	int result;
	clrscr();
	printf("\n enter the plain text:");
	scanf("%s",plain);
	printf("\n enter the key value:");
	scanf("%d",&key);
	printf("\n \n \t plain text: %s",plain);
	printf("\n \n \n encrypted text:");
	for(i=o,length=strlen(plain);i<length;i++)
	{
		
	}
}
